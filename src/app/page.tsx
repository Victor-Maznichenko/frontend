import { Intro } from '@/components/modules/intro';

const HomePage = () => {
   return (
      <div>
         <div className="noise" />
         <Intro />
      </div>
   );
};

export default HomePage;
