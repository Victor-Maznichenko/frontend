import { Intro } from '@/components/modules/intro';

const HomePage = () => {
   return (
      <main>
         <div className="noise" />
         <Intro />
      </main>
   );
};

export default HomePage;
